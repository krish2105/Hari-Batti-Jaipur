node_modules/
.next/
dist/
.expo/
.venv/
__pycache__/
*.pyc
.env
.env.*
!.env.example
.DS_Store
data/raw_video/
data/osm/*.osm
*.sumocfg.bak
*.tsbuildinfo
.pytest_cache/
.ruff_cache/
services/sim/build/

# CONFIDENTIAL survey data — never commit (the GitHub repo is public). Only aggregates are pushed.
data/raw/
data/processed/tmc_clean.csv
data/osm/

# Vercel CLI project link and build output
.vercel/

# Private strategy, outreach and build prompts — never in the public repo
docs-private/

# Playwright output
test-results/
playwright-report/

# Computer vision downloads and outputs (weights, datasets, videos) — large, fetched by services/cv
services/cv/vendor/
services/cv/weights/
services/cv/data/
services/cv/out/
*.mp4

# Data connectors (P8 W12): the real config names police/vendor endpoints; feed recordings and imported
# timing proposals can hold police data. Only the example config and synthetic fixtures are committed.
config/connectors.yaml
data/connector_drop/
data/feed_recordings/
data/signal_timings.proposed.csv

# Uploaded junction videos and their outputs (P8 W15): confidential, never committed
data/uploads/

# production env example for the hosting stack (P8 W18); the real infra/.env.production stays ignored
!infra/.env.production.example
