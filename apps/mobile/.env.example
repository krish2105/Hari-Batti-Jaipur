# HariBatti mobile app settings (copy to .env; .env is gitignored).
# The API the app reads live signals from and sends citizen reports to. On a phone, use your
# laptop's LAN address (ipconfig getifaddr en0), e.g. http://192.168.1.20:8000
EXPO_PUBLIC_API_URL=http://localhost:8000
