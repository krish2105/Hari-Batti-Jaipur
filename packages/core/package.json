{
  "name": "@haribatti/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types (Junction, PhaseState, DataSource) and GLOSA speed advice.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "devDependencies": {
    "vitest": "5.0.1"
  }
}
