{
  "name": "@haribatti/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared design tokens (signal colours, source badge colours, fonts).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@haribatti/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "5.0.1"
  }
}
